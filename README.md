# Photo-editor
``` ⬡ PhotoBender — browser-based photo editor in a single HTML file. Lightroom-style controls: exposure, HSL, color grading, crop with aspect ratios, presets + .xmp import, before/after split, and full-res PNG export. Zero installs, zero dependencies. Open the file and edit. ```  260 chars — fits comfortably.
